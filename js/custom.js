//  ============  custom js start  ==========  //

// aos animation

AOS.init();
$(document).on('ready', function () {
  // initialization of aos
  AOS.init({
    duration: 2000,
    once: true
  });
});